
/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};

/* PowerFox compat: requires macOS 10.8. */
enum {
  kLowerCaseType = 37,
  kLowerCaseSmallCapsSelector = 1,
};
